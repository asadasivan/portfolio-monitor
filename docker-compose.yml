services:
  portfolio-monitor:
    build: .
    image: portfolio-monitor:local
    working_dir: /app
    volumes:
      - ./config:/app/config
      - ./input:/app/input
      - ./data:/app/data
      - ./reports:/app/reports
    command: ["--help"]
